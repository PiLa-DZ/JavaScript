```js
const response = await fetch("http://localhost:3000/users");

// response.ok
// Returns true only if the status code is between 200 and 299.
if (response.ok) {
  const data = await response.json();
  console.log(data);
}
```
