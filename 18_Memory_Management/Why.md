Memory Management is the difference between 
a system that runs for months without a reboot 

and a system that hits an OOM (Out of Memory) crash 
because of a "memory leak."

### ============================================================
JavaScript handles memory automatically using a Garbage Collector, 
but you still need to understand how the "RAM" is structured 
so you don't accidentally fill it up with orphaned data.


